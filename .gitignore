.venv/
__pycache__/
*.pyc
.pytest_cache/
src/edf.egg-info/

# Secrets
.env
*.env

# Local data caches (regenerated by the pipeline). artifacts/ (trained models +
# backtest reports) IS committed intentionally so the scheduled retrain
# workflow has something to diff/update.
data/raw/
data/processed/
